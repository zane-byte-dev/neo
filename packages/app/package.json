{
    "name": "@neo/app",
    "version": "0.1.0",
    "description": "Neo application server: HTTP routes, CLI, web frontend.",
    "type": "module",
    "private": true,
    "main": "./dist/main.js",
    "types": "./dist/main.d.ts",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "clean": "rm -rf dist",
        "dev": "tsx src/main.ts",
        "start": "node dist/main.js"
    },
    "dependencies": {
        "@earendil-works/pi-tui": "^0.80.3",
        "@neo/agent": "*",
        "@neo/runtime": "*",
        "diff": "^9.0.0"
    }
}
