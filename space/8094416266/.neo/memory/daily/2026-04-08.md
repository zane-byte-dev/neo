# 2026-04-08

## 今日要点
- M4 节点环境已就绪，PM2 运行中。
- 讨论了 LiteLLM 作为模型网关的配置，屏蔽本地 MLX 模型和云端 API 的差异。
- 编写了 `config.yaml` 配置文件模板，用于统一管理模型。
- 讨论了 M4 节点后续向量库的选型逻辑。

## 关键决策
- 优先推进 LiteLLM 配置。

## 待跟进
- 在 M4 上完成 `pip install 'litellm[proxy]'`。
- 根据实际需求配置 `config.yaml` 中的模型映射。
- 使用 PM2 守护进程确保 LiteLLM Proxy 在后台持续运行。
- 在 Air 上验证连接。

## 原始碎片
- "建议先从 **LiteLLM** 入手，作为统一的模型网关，屏蔽本地 MLX 模型和云端 API 的差异。"
- "既然你现在不在那台 M4 或 Air 面前，我们先停下 CLI 的配置操作。"

---
*由 inkClaw Session-to-Log 自动生成 · 2026-04-08*